@model IEnumerable<OracleMVCTest.Models.EQUIPMENT>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EQUIPMENT_NAME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QUANTITY)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EQUIPMENT_NAME)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QUANTITY)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EQUIPMENT_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EQUIPMENT_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EQUIPMENT_ID })
        </td>
    </tr>
}

</table>
